# Visualization using tart for Apple Silicon

Tart is a package that lets you create Linux/MacOS CLI VMs. This directory contains scripts that are using for building such machines in order to test different vulnerabilities. The machines are plain, the only modification being made is the new 'test' user with sudo privileges.

Connect to machines via:
ssh -p 22 test@<IP_PORT>